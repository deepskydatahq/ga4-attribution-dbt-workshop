ga4_attribution:
  target: dev
  outputs:
    dev:
      type: bigquery
      method: oauth
      project: "{{ env_var('GCP_PROJECT', 'your-gcp-project') }}"
      dataset: ga4_attribution_dev
      threads: 4
      timeout_seconds: 300
      location: US
